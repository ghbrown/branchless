#the line numbers of the contents are important in this file,
#since the uncommented info is accessed by line rather than parsing
#directory names (alphabetical order)
fortran   python

#official langauge names (match with above)
Fortran   Python
